{
  "name": "serverless",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "twilio-run",
    "deploy": "twilio-run deploy"
  },
  "dependencies": {
    "crypto-js": "^4.0.0",
    "js-base64": "^3.6.0",
    "node-fetch": "^2.6.1",
    "request": "^2.88.2",
    "voiceit2-nodejs": "^2.5.0"
  },
  "devDependencies": {
    "twilio-run": "^2.6.0"
  },
  "engines": {
    "node": "10"
  }
}
